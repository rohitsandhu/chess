{{ form_start(form) }}
    {{ form_errors(form) }}

<div class="container align-middle divLogin divCrearTorneig ">
    <div class="row customLoginRow " style="margin: 0">
        <div class="col-lg-10 loginFormCustom rounded shadow p-4">

            <div class="form-group mt-3 p-2">
                <div class="col-12">
                    {{ form_row(form.nom) }}
                </div>
            </div>

            <div class="form-group p-2" >
                <div class="col-12">
                    {{ form_row(form.cognoms) }}
                </div>
            </div>

            <div class="form-group p-2">
                <div class="col-12 ">
                    {{ form_row(form.equip) }}
                </div>
            </div>

            <div class="form-group p-2">
                <div class="col-12">
                    {{ form_row(form.pais) }}
                </div>
            </div>

            <div class="form-group p-2">
                <div class="col-12">
                    {{ form_row(form.elo) }}
                </div>
            </div>
            {#            {% do form.llistaJugadors.setRendered %}#}
            <button class="btn btn-light">{{ button_label|default('Save') }}</button>
        </div>

    </div>
</div>

{{ form_end(form) }}
